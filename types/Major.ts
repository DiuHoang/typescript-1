export interface Major {
    id: number
    code: string
    name: string
    fee: number
    duration: number
    description?: string
    isEnroll?: boolean
    image?: string
    numberOfStudents?: number
}
